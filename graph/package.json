{
  "name": "demo-oracle",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --ipfs https://ipfs.network.thegraph.com demo-oracle",
    "create-local": "graph create --node http://localhost:8020/ demo-oracle",
    "remove-local": "graph remove --node http://localhost:8020/ demo-oracle",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 demo-oracle",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.97.1",
    "@graphprotocol/graph-ts": "0.37.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
